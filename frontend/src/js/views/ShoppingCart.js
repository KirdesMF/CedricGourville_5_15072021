import { useLocalStorage } from '../utils/local-storage';

export const ShoppingCart = {
   render: () => {
      return `
         <section>
            <h2>Panier</h2>
         </section>
      `;
   },
};
